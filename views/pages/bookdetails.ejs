<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="/styles/main.css">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
        <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    </head>
     
    <body style="padding-top: 70px;">
        <div class="container"> 
            <header style="position: fixed; top: 0; left: 0; width: 100%; background-color: black; z-index: 1000;">
                <%- include('../partials/header') %>
            </header>
    
            <div id="book-details">
                <h1><%= bookTitle %></h1>
                <p>著者: <%= author || '著者不明' %></p>
                <p>出版社: <%= publisherName || '出版社不明' %></p>
                <p><%= itemCaption || '説明なし' %></p>
                <% if (largeImageUrl) { %>
                    <img src="<%= largeImageUrl %>" alt="表紙画像">
                <% } else { %>
                    <p>表紙画像がありません。</p>
                <% } %>
            </div>
            
            <%- include('../partials/geocode-form'); %>
        </div>
        
    
        <footer class="mt-4 w-100">
            <%- include('../partials/footer') %>
        </footer>

        <script src="/scripts/geocode.js"></script>
        <!-- <script src="/scripts/searchlibrary.js"></script> -->

    </body>
    
</html>
